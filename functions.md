Use arrow functions across the entire application.

No default exports UNLESS: framework required.

Design pattern:

A function with more than 2 positional params is just a bug maker. Any function that needs more gets a single params options object.
