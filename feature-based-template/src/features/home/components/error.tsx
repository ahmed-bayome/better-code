/**
 * If somehow the feature need its own error ui, rare condition 
 * but in case thats true, am here
 * most of the apps it will be one global error ui
*/

export const HomeError = () => {
  // JSX
};